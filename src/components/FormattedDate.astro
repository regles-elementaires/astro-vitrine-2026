---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('fr-fr', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
